[project]
name = "sayrhazi"
version = "0.4.2"
description = "Workflow multi-agents portable (stdlib uniquement, zero dependance)"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
sayrhazi-cli = "main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
