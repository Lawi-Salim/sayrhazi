# Commande `update sayrhazi` (fonction de profil PowerShell).

Mise à jour via `engine/updater.py` : agents + watcher recopiés, migration
douce de la config (clés manquantes, backup `history/`), version alignée,
orphelins signalés, check relancé. Config, rapports et historique préservés.
